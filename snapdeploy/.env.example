# Optional local override. The requested token is already embedded in the image.
# ACCESS_TOKEN=another_token

# Ports (both must differ)
# PORT=3000                # public notes website / container port
# WORKER_HEALTH_PORT=3100  # background worker's internal (loopback) health server
