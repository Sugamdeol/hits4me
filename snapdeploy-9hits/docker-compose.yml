services:
  notesly:
    build: .
    environment:
      ACCESS_KEY: "${ACCESS_KEY:-23f9097a8d823267188c49b3cc0598b1}"
      PORT: "3000"
    ports:
      - "3000:3000"
    tmpfs:
      - /tmp
      - /dev/shm
    restart: unless-stopped
